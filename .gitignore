.DS_Store
*.pyc
__pycache__/
.env
.claude/settings.local.json
*.log
venv/
node_modules/

# Registro operativo con datos reales — se genera desde plantillas/registry.json
# Solo raiz: examples/playbook_registry.json es ficticio y SI se versiona
/playbook_registry.json

# Datos reales — NUNCA versionar. Bloqueo por nombre exacto (no patron amplio)
# El kit no tiene observatorio/ con datos reales; archivos de ejemplo SI se versionan
**/snapshot*.json
**/dashboard_data*.js
